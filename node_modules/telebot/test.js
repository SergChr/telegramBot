const TOKEN = '73107572:AAHCngunN3487qyP3rWhYzwX9UPEQSO80mI';
const USER = 37980985;

const TeleBot = require('.');

const bot = new TeleBot({
    token: TOKEN,
    usePlugins: ['askUser'],
    pluginConfig: {
    }
});

bot.on('/start', msg => {
    return bot.sendMessage(msg.from.id, 'START').then((response) => {
        console.log(response);
        return bot.deleteMessage(msg.chat.id, response.result.message_id);
    });
});

// bot.on('text', msg => msg.reply.text('text'));
// bot.on('edit', msg => msg.reply.text('edit'));
//
// bot.on('update', (data) => {
//     console.log('UPDATE', data);
// });

bot.start();
